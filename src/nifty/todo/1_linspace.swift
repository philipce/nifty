// extend this to also allow for the matlab equivalent of 
// 0:.1:1 = [0 .1 .2 ... 1]? Or is there a way to already do that for free
// with swift range? Or should that go in a different function, eg rang()?
